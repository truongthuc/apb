# Naming Conventions

## Conventions

- Use English filenames and identifiers.
- Store agent-facing planning files in `.agent/planning/`.
- Store agent-facing documentation in `.agent/docs/`.
- Store architecture decisions as individual ADR files in `.agent/architecture-decisions/`.
- Use root `AGENTS.md` and `CLAUDE.md` only as bridge files.
